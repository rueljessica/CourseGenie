<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    @entryPointStyles('app')
    @entryPointScripts('app')
    <script>
        // On page load or when changing themes, best to add inline in `head` to avoid FOUC
        if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
        } else {
          document.documentElement.classList.remove('dark')
        }
    </script>
  </head>
    @!section('body')
    
    @include('layouts/sidebar')

    <div class="bg-white dark:bg-gray-900 p-4 sm:ml-64">
      <div class="h-screen p-4 border-2 border-gray-200 border-dashed rounded-lg dark:border-gray-700 mt-20">
         <p class="select-none text-3xl pt-1 pl-7 text-gray-900 dark:text-white"> < Perfil /> </p>
         <div class="grid grid-cols-3 m-3 flex-shrink-0">
            <div class="flex pl-5 pt-5 h-96 rounded bg-gray-50 dark:bg-gray-800">
               <div class="relative overflow-hidden bg-gray-100 rounded w-36 h-36 dark:bg-gray-600">
                  <img class=" rounded w-36 h-36" src="{../../public/img/img.png}">
               </div>
               <div class="mr-10">
                  <p class="pl-10 pt-5 text-2xl text-gray-600 dark:text-gray-400">{{auth.user.name}}</p>
                  <p class="pl-10 pt-2 text-xl text-gray-600 dark:text-gray-400">E-mail: {{auth.user.email}}</p>
                  <p class="pl-10 pt-2 text-xl text-gray-600 dark:text-gray-400">Matrícula: {{auth.user.matricula}}</p>
               </div>
            </div>
         </div>
      </div>
   </div>
   <script src={{"../path/to/flowbite/dist/flowbite.min.js"}}></script>
</html>